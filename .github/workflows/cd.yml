# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
   #   - uses: actions/checkout@v3
      - run: flutter build web --dart-define URL_BASE=https://api.martinlog.com.br --dart-define APP_NAME="Plataforma Martin log"
      - run: firebase deploy --only hosting:martinlog
      - run: cd build/web && npx surge --project . --domain martinlog.surge.sh

   #   - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MARTINLOG_WEB }}'
          channelId: live
          projectId: martinlog-web
